import { TariffIconPipe } from './tariff-icon.pipe';

describe('TariffIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TariffIconPipe();
    expect(pipe).toBeTruthy();
  });
});
