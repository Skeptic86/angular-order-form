import { ChoseActiveCardPipe } from './chose-active-card.pipe';

describe('ChoseActiveCardPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoseActiveCardPipe();
    expect(pipe).toBeTruthy();
  });
});
