import { PaymentIconPipe } from './payment-icon.pipe';

describe('PaymentIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentIconPipe();
    expect(pipe).toBeTruthy();
  });
});
